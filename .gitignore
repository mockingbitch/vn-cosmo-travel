*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json

# Composer / PHP deps (cài lại trên server: composer install --no-dev)
/vendor

# Node (hosting không chạy Node; bản build đã commit trong public/build)
/node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
.npm
.pnp.*

# Laravel Vite dev
/public/hot

# --- Chỉ dùng máy dev / Docker / QA; không cần trên production ---
/docker/
/docker-compose.yml
/tests/
phpunit.xml
database/factories/

# Báo cáo & cache công cụ
coverage/
.php-cs-fixer.cache
.phpstan.cache

# DB / dump cục bộ
*.sqlite
*.sqlite-journal

# Storage & app runtime
/public/storage
/storage/*.key
/storage/pail

# Committed for hosts without Node — chạy `npm run build` khi đổi CSS/JS rồi commit public/build.
# /public/build

_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db
